spring.datasource.url=${TEST_DB_URL:jdbc:postgresql://localhost:5433/ecommerce_test}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=${TEST_DB_USERNAME:ecommerce_app}
spring.datasource.password=${TEST_DB_PASSWORD:ecommerce_app_password}

spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

app.environment=test
app.payment-provider=fake
app.authentication-provider=test
app.stripe.secret-key=
